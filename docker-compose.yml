services:
  app:
    build:
      context: .
    image: youtube-local-downloader:latest
    container_name: youtube-local-downloader
    init: true
    restart: unless-stopped
    ports:
      - "5000:5000"
    environment:
      TZ: America/Sao_Paulo
    volumes:
      - ./downloads:/app/downloads
